name: Stats

on:
  push:
    branches: [main]

permissions:
  contents: write  # Permiso necesario para realizar push automático

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then
            echo "📦 Instalando dependencias..."
            pip install -r requirements.txt
          else
            echo "⚠️ No se encontró requirements.txt, saltando instalación."
          fi

      - name: Ejecutar stats.py
        run: |
          echo "📊 Ejecutando stats.py desde la raíz..."
          python ./stats.py || echo "⚠️ El script falló, pero el flujo continúa"

      - name: Mostrar estado del repositorio
        run: |
          echo "📂 Verificando cambios realizados..."
          git status
          echo "📁 Archivos en la raíz:"
          ls -la

      - name: Commit y push de cambios
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Update stats
          skip_dirty_check: true
          push: true


